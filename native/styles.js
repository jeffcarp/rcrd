import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  basicPage: {
    marginTop: 20,
    flexDirection: 'column',
    padding: 8,
    flex: 1
  },
  input: {
    height: 40,
    borderColor: '#ccc',
    borderWidth: 1,
    padding: 4,
    marginBottom: 12
  }
})

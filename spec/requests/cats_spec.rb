require 'spec_helper'

describe "Cats" do

  describe "GET /cats/:name" do
=begin
    it "loads" do
      user = User.create!(email: "whatever@jeff.is", password: "test", password_confirmation: "test")
      req_one = user.records.create!(raw: "workout, swim, 3200 yards", target: Time.now)
      get "/cats/swim"
      expect(response.status).to eq(200)
    end
=end
  end

end
